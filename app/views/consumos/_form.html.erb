<%= form_for(@consumo) do |f| %>
  <% if @consumo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumo.errors.count, "error") %> prohibited this consumo from being saved:</h2>

      <ul>
      <% @consumo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
   <div class="field">
   <%= f.label :cliente %><br />
   <%= f.collection_select :cliente_id, Cliente.all, :id, :nome, :include_blank => 'Selecione um Cliente' %>
  </div>
  <div class="field">
   <%= f.label :produto %><br />
   <%= f.collection_select :produto_id, Produto.all, :id, :descricao, :include_blank => 'Selecione um Produto' %>
  </div>
  <div class="field">
    <%= f.label :quant %><br />
    <%= f.text_field :quant %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
